{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "python diagnosis_script.py && streamlit run streamlit_dashboard.py --server.port $PORT --server.address 0.0.0.0"
  }
}
